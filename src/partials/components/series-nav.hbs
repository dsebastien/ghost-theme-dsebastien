{{!--
    Series navigation dispatcher (articles plan Tier 2.5).

    One branch per series, each a call to `series-branch` (which renders the
    strip only if THIS post carries the series's internal `#series-<slug>` tag).
    Detection uses an id+tag query, NOT {{#has}} — {{#has tag=}} does not match
    internal-visibility tags in this Ghost version. See series-branch.hbs.

    Empty until the first series exists — renders nothing, zero visual impact.

    CONTRACT (kept in sync by the `developassion-publish` skill + src/data/series.json):
      * Membership + order come from the `#series-<slug>` internal tag applied to
        posts in Ghost; order = publish date. Adding a post to an EXISTING series
        needs ONLY the tag — no edit here, the strip fetches members dynamically.
      * Creating a NEW series adds ONE branch below, between the INSERT markers.
        Keep the markers intact and one branch per line.
--}}
{{!-- SERIES-BRANCHES:START — developassion-publish inserts new-series branches here --}}
{{> "components/series-branch" seriesTag="hash-series-git-tutorial" seriesTitle="Git tutorial for beginners"}}
{{> "components/series-branch" seriesTag="hash-series-journaling" seriesTitle="Journaling every day is powerful"}}
{{> "components/series-branch" seriesTag="hash-series-typescript" seriesTitle="What's new in TypeScript"}}
{{> "components/series-branch" seriesTag="hash-series-team-management" seriesTitle="Team management tips"}}
{{!-- SERIES-BRANCHES:END --}}
